# SDK Demo (mock errors)

This demo was moved to **`examples/sdk-demo/`**.

It triggers synthetic errors (ReferenceError, eval SyntaxError, etc.) for testing SDK capture only.

For the **thesis live demo** (real bugs → source maps → GitHub PR), use **`examples/live-demo/`** and read **`docs/LIVE-DEMO-WALKTHROUGH.md`**.
